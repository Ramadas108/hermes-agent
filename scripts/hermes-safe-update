#!/usr/bin/env python3
import os
import sys
sys.path.insert(0, "/home/openclaw/apps/hermes-agent")
from hermes_cli.git_update import main, safe_update_stack
if "--self-test" in sys.argv:
    raise SystemExit(main())
raise SystemExit(safe_update_stack(os.environ.get("HERMES_REPO", "/home/openclaw/apps/hermes-agent")))
